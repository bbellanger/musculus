# Python
__pycache__/
*.pyc
*.pyo
*.pyd

# Django
db.sqlite3
*.log
media/

# Environment
.env
.venv/
venv/
env/

# OS
.DS_Store
Thumbs.db

# Docker build artifacts
.dockerignore     # keep this out if it contains paths you don't want exposed

# Dcoker volume data
db_data/

# Production environment file (if you keep separate ones)
.env.production
.env.local

# SQLite database files (already there, but be explicit)
*.sqlite3

# Collected static files — regenerated by collectstatic at deploy time
staticfiles/

# Nginx logs if you ever mount them locally
nginx.conf
*.log
logs/

# Docker volumes if you ever snapshot them
*.tar
